<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Teams</title>
  </head>
  <body>
    <h1><%=@team.name%></h1>
    <h2><%=@team.motto%></h2>
    <% @heros.each do |hero|
    <h3><%=hero.name%></h3>
    <% end %>
  </body>
</html>
